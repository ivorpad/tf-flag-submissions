# Migration `20200703024905-add-items`

This migration has been generated by Ivor Padilla at 7/3/2020, 2:49:05 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Author" (
"username" text  NOT NULL ,
    PRIMARY KEY ("username"))

CREATE TABLE "public"."Item" (
"authorUsername" text   ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"itemName" text  NOT NULL ,"submittedAt" timestamp(3)   ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Reason" (
"authorId" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"reason" text  NOT NULL ,"resolved" boolean  NOT NULL DEFAULT false,"reviewerId" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Reviewer" (
"username" text  NOT NULL ,
    PRIMARY KEY ("username"))

ALTER TABLE "public"."Item" ADD FOREIGN KEY ("authorUsername")REFERENCES "public"."Author"("username") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Reason" ADD FOREIGN KEY ("authorId")REFERENCES "public"."Author"("username") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Reason" ADD FOREIGN KEY ("reviewerId")REFERENCES "public"."Reviewer"("username") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200702140924-add-tables..20200703024905-add-items
--- datamodel.dml
+++ datamodel.dml
@@ -3,37 +3,39 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
+model Author {
+  username String   @id
+  Item     Item[]
+  reason   Reason[]
+}
+
 model Item {
-  id Int @id @default(autoincrement())
-  itemName String
-  submittedAt DateTime?
-  createdAt DateTime @default(now())
-  updatedAt DateTime @default(now())
+  authorUsername String?
+  createdAt      DateTime  @default(now())
+  id             Int       @default(autoincrement()) @id
+  itemName       String
+  submittedAt    DateTime?
+  updatedAt      DateTime  @default(now())
+  author         Author?   @relation(fields: [authorUsername], references: [username])
 }
-model Author {
-  username String @id
-  reason Reason[]
-  item Item[]
+model Reason {
+  authorId   String
+  createdAt  DateTime @default(now())
+  id         Int      @default(autoincrement()) @id
+  reason     String
+  resolved   Boolean  @default(false)
+  reviewerId String
+  updatedAt  DateTime @default(now())
+  author     Author   @relation(fields: [authorId], references: [username])
+  reviewer   Reviewer @relation(fields: [reviewerId], references: [username])
 }
 model Reviewer {
-  username String @id
-  reason Reason[]
-}
-
-model Reason {
- id Int @id @default(autoincrement())
- author Author @relation(fields: [authorId], references: [username])
- authorId String
- reviewer Reviewer @relation(fields: [reviewerId], references: [username])
- reviewerId String
- reason String
- resolved Boolean @default(false)
- createdAt DateTime @default(now())
- updatedAt DateTime @default(now())
+  username String   @id
+  reason   Reason[]
 }
```


